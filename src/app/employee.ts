export interface Employee {
    name: string;
    email: string;
    address: string;
    phone: number;
}

export interface Events {
    name: string;
    description: string;
    date: string;
}

export interface SpecialEvents {
    name: string;
    description: string;
    date: string
}